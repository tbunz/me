# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# This project uses bun — never commit npm/yarn/pnpm lockfiles
# (a stray package-lock.json makes Vercel run `npm install` instead of bun)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
.claude

# Local env files
.env
.env.*
!.env.example
